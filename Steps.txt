Jenkins:

    Update plugins & Restart service to solve the issue

    Tools:

        jdk
        ✅ Install automatically
            Install from adoptium.net
                Version
                jdk-21.0.3+9

        nodejs
        ✅ Install automatically
            Install from nodejs.org
                Version
                NodeJS 25.3.0

        DP-Check
        ✅ Install automatically
            Install from github.com
                Version
                dependency-check 12.2.0

        docker
        ✅ Install automatically
            Download from docker.com
                Docker version
                latest

        sonar-scanner
        ✅ Install automatically
            Install from Maven Central
                Version
                SonarQube Scanner 8.0.1.6346

    Credentials:

        SonarQube
            Kind
                Secret text
            Secret
                ••••••••••••••••••••••••••••••••••••••••••••
            ID
                sonar-token
            Description:
                sonar-token

        OWASP
            Kind
                Secret text
            Secret
                ••••••••••••••••••••••••••••••••••••••••••••
            ID
                nvd-api-key
            Description:
                nvd-api-key

        Github

            Kind
                Secret text
            Secret
                ••••••••••••••••••••••••••••••••••••••••••••
            ID
                github
            Description:
                github

        Docker

            Kind
                Username with password
            Username
                freelancerdev
            Password
                •••••••••••
            ID
                docker
            Description:
                docker

    System:

        SonarQube installations
        
            Name
                sonar-server
            Server URL
                http://192.168.57.12:9000/
            Server authentication token
                sonar-token



Setup K8:

    awslocal eks update-kubeconfig --region us-east-1 --name Tetris-EKS-Cluster
    kubectl create namespace tetris
    kubectl create namespace argocd
    kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/v2.4.7/manifests/install.yaml
    Wait 50 seconds

Setup port-forward:

    kubectl patch svc argocd-server -n argocd -p '{"spec":{"type":"LoadBalancer"}}'
    kubectl get svc -n argocd                 
    kubectl port-forward svc/argocd-server -n argocd 8080:443

Password Argocd:

    export ARGOCD_SERVER='kubectl get svc argocd-server -n argocd -o json | jq — raw-output ‘.status.loadBalancer.ingress[0].hostname’'
    export ARGO_PWD=`kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d`
    echo $ARGO_PWD

    User: admin
    Password: echo $ARGO_PWD

Join Tetris:

    kubectl get svc -n tetris
    kubectl port-forward svc/tetris-service -n tetris 3000:80

Changes Manifest:

    spec:
      containers:
      - name: tetris
        image: freelancerdev/tetrisv2:1       <--------------------
        ports:
        - containerPort: 3000

Check Artifacts:

    docker volume inspect jenkins_container_jenkins-vol
    cd  Mountpoint": "/var/lib/docker/volumes/jenkins_container_jenkins-vol/_data"

Test Version 2:

    git restore .
    git clean -fd
    git checkout -b develop
    rsync -av --delete Tetris-V2/ Tetris-V1/
